## 1. Milestone 1 — Twelve Data provider and user settings foundation

- [x] 1.1 Add the `TWELVE_DATA` provider, `TWELVE_DATA_STOCK_QUOTE` external tool, and `ToolType.api_stock_quote`, and include them in the provider/tool catalogs with the BASIC-plan call cost represented without adding a Python SDK.
- [x] 1.2 Add `platform_twelve_data_api_key: SecretStr` to `Config`, load it specifically from `TWELVE_DATA_API_KEY`, include it in secret redaction, and update the existing config tests without reading or printing the stored real key.
- [x] 1.3 Add encrypted `twelve_data_api_key` and nullable `tool_choice_api_stock_quote` fields across `UserDB`, the user domain model, both mapper directions and in-place apply path, and profile-connect merge behavior.
- [x] 1.4 Extend access-token resolution, tool-choice resolution, intelligence preset defaults/models, user settings payload/response models, API mappers, and provider/tool listings so user, sponsor, and platform Twelve Data credentials follow the existing precedence and accounting behavior, while deterministic system agents use platform credentials without duplicated database secrets or credits.
- [x] 1.5 Update `docs/open-api-docs.yaml` for the Twelve Data key and stock-quote tool choice, including masked response behavior, and verify settings save/fetch round trips remain backward compatible.
- [x] 1.6 Update relevant existing tests for provider catalogs, config, token/tool resolution, presets, user mapping, profile merging, settings mapping, and API documentation; keep tests offline, and stop to ask the user before creating any new test file or introducing extensive mocking.
- [x] 1.7 Confirm the user model remains imported by `src/db/alembic/env.py`, then ask the user to run `./tools/db_generate_migration -y`; inspect the generated migration for only the intended nullable user columns and do not hand-write a replacement migration.
- [x] 1.8 Run Ruff and the spacing checker with fixes on every changed Python file, run the relevant offline tests, validate the OpenAPI document, and run `git diff --check`; resolve all milestone regressions.
- [x] 1.9 **TODO — STOP for the user's manual code and generated-migration review. Do not begin Milestone 2 until the user explicitly approves this milestone; the user will run `./tools/db_apply_migration` only as part of that approval and may commit the milestone as one unit.**

## 2. Milestone 2 — Unified asset-price lookup and stock quotes

- [x] 2.1 Add dedicated stock quote/result types and a `StockQuoteFetcher` that resolves `api_stock_quote`, calls Twelve Data `/quote` through `tracked_web_fetcher`, preserves native currency and exchange/MIC/timestamp/market metadata, and never logs or returns the API key.
- [x] 2.2 Add dedicated error codes and map Twelve Data validation, ambiguity, not-found, rate-limit, null/empty/array/malformed, and provider failures to the appropriate structured project errors while forwarding only safe provider guidance to the LLM and chaining caught exceptions.
- [x] 2.3 Add a dedicated asset-price service implementing normalized markers, explicit `fiat|crypto|stock` validation, omitted-type inference in fiat/crypto/stock order, stock exchange qualifiers, amount handling, and native-to-requested fiat/crypto conversion through `ExchangeRateFetcher`.
- [x] 2.4 Normalize successful results to include asset/type, amount, requested currency, converted unit price and value, plus stock-native price/currency, provider-resolved identity, exchange/MIC, timestamp, and market-open status.
- [x] 2.5 Change fiat/cryptocurrency and stock price caches to nine minutes, add force-refresh behavior to `WebFetcher`, the usage-tracking decorator and DI factories, and propagate `force` through every exchange/quote leg so forced requests bypass reads but overwrite the normal cache entries.
- [x] 2.6 Replace the LLM-visible `get_exchange_rate` function with `get_asset_price(asset, currency, asset_type=None, force=False, amount=None)`, update all tool registration/schema references, document collision-safe explicit typing and explicit-user-refresh-only force usage, and retain the current JSON success/error envelope behavior.
- [x] 2.7 Wire the new quote and asset-price services through DI and update any affected external-tool, accounting, and cleanup/cache integration paths without adding batching or commodity support.
- [x] 2.8 Update relevant existing offline tests for routing, type inference and explicit overrides including an `AAPL` collision, native and converted stock quotes, amount calculations, metadata, structured errors, nine-minute caching, force bypass at both cache layers, tracking, and the LLM tool catalog; ask before creating any new test file or introducing extensive mocking.
- [x] 2.9 Run Ruff and the spacing checker with fixes on every changed Python file, run all relevant offline currency/web-fetcher/DI/LLM-tool tests, and run `git diff --check`; resolve all milestone regressions.
- [x] 2.10 **TODO — STOP for the user's manual code review. Do not begin Milestone 3 until the user explicitly approves this milestone and may commit it as one unit.**

## 3. Milestone 3 — Generalized fiat, crypto, and stock price alerts

- [x] 3.1 Generalize the `PriceAlert` domain/DB model and mapper from currency-pair fields to `asset_type`, normalized `asset_id`, and requested `currency`, with composite identity `(chat_id, asset_type, asset_id, currency)` and all existing ownership, threshold, last-price, and timestamp data retained.
- [x] 3.2 Generalize the price-alert repository, service result models, responder, scheduler wiring, and create/delete/list/evaluate operations to use the unified asset-price service with `force=false` and owner-scoped DI credential resolution.
- [x] 3.3 Replace the three currency-specific LLM alert functions with three asset-aware equivalents using the same optional type inference and explicit override semantics as `get_asset_price`, while preserving admin authorization and messaging-frequency behavior.
- [x] 3.4 Deduplicate equivalent normalized lookups during each alert evaluation run or rely on the shared nine-minute entry where credential scope permits, keep single-symbol Twelve Data calls, update stored comparison data only on trigger, and isolate safe structured failures per alert.
- [x] 3.5 Confirm the price-alert model remains imported by `src/db/alembic/env.py`, then ask the user to run `./tools/db_generate_migration -y`; complete and inspect the generated migration so all existing bases become literal `crypto`, existing pair data is preserved, and downgrade handling is explicit without runtime model imports.
- [x] 3.6 Update relevant existing offline tests for migrated currency alerts, exchange-qualified stock identities, CRUD/admin behavior, threshold evaluation, per-owner credentials, deduplication/cache reuse, responder output, and failure isolation; ask before creating any new test file or introducing extensive mocking.
- [x] 3.7 Update affected OpenAPI or operational documentation, run Ruff and the spacing checker with fixes on every changed Python file, run all relevant offline alert/repository/scheduler/LLM-tool tests, validate migration upgrade/downgrade behavior, and run `git diff --check`; resolve all milestone regressions.
- [x] 3.8 **TODO — STOP for the user's manual code and generated-migration review. The change is not complete until the user explicitly approves this milestone; the user will run `./tools/db_apply_migration` only as part of that approval and may commit the milestone as one unit.**
